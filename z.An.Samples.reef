context App::J1: string
post:

    let json: JsonObject = {
                title:          "Sky",
                colors:     ['Red', 'Green', {x: 1, y:2}] ,
                pos: {x: 3, y: 4}
            }
    let res: string = json.ToString

    result = res
